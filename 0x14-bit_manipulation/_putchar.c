#include <stdio.h>

/**
 * _putchar - writes charecter to standard outpit
 * @c: character to be written
 *
 * Return: number of character written on success
 * 1 is returned and errno i set correctly on  error
 */

int _putchar(char c)
{
putchar(c);
return (1);
}

